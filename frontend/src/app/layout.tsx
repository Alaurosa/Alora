import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Alora - AI-Powered Lifestyle Optimization for Women's Cycles",
  description: "Get personalized daily recommendations that adapt to your menstrual cycle phase, mood, and energy levels. Optimize your lifestyle with AI-powered insights.",
  keywords: "menstrual cycle, women's health, AI recommendations, lifestyle optimization, cycle tracking",
  authors: [{ name: "Alora Team" }],
};

export const viewport = {
  width: 'device-width',
  initialScale: 1,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
